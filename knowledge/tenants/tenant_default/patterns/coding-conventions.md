# Coding Conventions

- Preserve existing public contracts when extending modules.
- Prefer additive migrations and avoid destructive schema changes.
- Keep orchestration explicit through typed workflow artifacts.
- Use deterministic verification gates (lint, test, build).
