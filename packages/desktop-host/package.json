{
  "name": "@cp/desktop-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/desktop-host/src/index.js",
  "types": "dist/desktop-host/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "pnpm run build && node --test dist/desktop-host/src/*.test.js",
    "start": "node dist/desktop-host/src/index.js"
  },
  "dependencies": {
    "@cp/domain": "workspace:*",
    "@cp/worker-local": "workspace:*"
  }
}
