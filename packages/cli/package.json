{
  "name": "@cp/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "devtools": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "devtools": "tsx src/index.ts"
  },
  "dependencies": {
    "@cp/worker-local": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  }
}
