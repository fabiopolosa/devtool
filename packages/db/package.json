{
  "name": "@cp/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests yet for @cp/db\"",
    "db:migrate": "tsx src/cli/migrate.ts"
  },
  "dependencies": {
    "@cp/domain": "workspace:*",
    "@cp/config": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "drizzle-kit": "^0.31.5",
    "tsx": "^4.20.6"
  }
}
